# Notes for Claude

- Weekly data updates: follow `ROUTINE.md` exactly. Always run `node scripts/validate.mjs` before committing `data/events.json`.
- Static site, no framework, no build step. Keep it dependency-free.
- Any new category/tag/label must be added to all three files in `i18n/`.
